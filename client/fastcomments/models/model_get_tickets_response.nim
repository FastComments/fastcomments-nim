#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_api_status
import model_api_ticket

type GetTicketsResponse* = object
  ## 
  status*: APIStatus
  tickets*: seq[APITicket]


# Custom JSON deserialization for GetTicketsResponse with custom field names
proc to*(node: JsonNode, T: typedesc[GetTicketsResponse]): GetTicketsResponse =
  result = GetTicketsResponse()
  if node.kind == JObject:
    if node.hasKey("status"):
      result.status = to(node["status"], APIStatus)
    if node.hasKey("tickets"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["tickets"]
      if arrayNode.kind == JArray:
        result.tickets = @[]
        for item in arrayNode.items:
          result.tickets.add(to(item, APITicket))

# Custom JSON serialization for GetTicketsResponse with custom field names
proc `%`*(obj: GetTicketsResponse): JsonNode =
  result = newJObject()
  result["status"] = %obj.status
  result["tickets"] = %obj.tickets

