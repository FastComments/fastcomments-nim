#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_api_status
import model_user_badge

type GetUserManualBadgesResponse* = object
  ## 
  badges*: seq[UserBadge]
  status*: APIStatus


# Custom JSON deserialization for GetUserManualBadgesResponse with custom field names
proc to*(node: JsonNode, T: typedesc[GetUserManualBadgesResponse]): GetUserManualBadgesResponse =
  result = GetUserManualBadgesResponse()
  if node.kind == JObject:
    if node.hasKey("badges"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["badges"]
      if arrayNode.kind == JArray:
        result.badges = @[]
        for item in arrayNode.items:
          result.badges.add(to(item, UserBadge))
    if node.hasKey("status"):
      result.status = to(node["status"], APIStatus)

# Custom JSON serialization for GetUserManualBadgesResponse with custom field names
proc `%`*(obj: GetUserManualBadgesResponse): JsonNode =
  result = newJObject()
  result["badges"] = %obj.badges
  result["status"] = %obj.status

