#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type Moderator* = object
  ## 
  id*: string
  tenantId*: string
  name*: Option[string]
  userId*: Option[string]
  acceptedInvite*: bool
  email*: Option[string]
  markReviewedCount*: float64
  deletedCount*: float64
  markedSpamCount*: float64
  markedNotSpamCount*: float64
  approvedCount*: float64
  unApprovedCount*: float64
  editedCount*: float64
  bannedCount*: float64
  unFlaggedCount*: float64
  verificationId*: Option[string]
  createdAt*: string
  moderationGroupIds*: Option[seq[string]]


# Custom JSON deserialization for Moderator with custom field names
proc to*(node: JsonNode, T: typedesc[Moderator]): Moderator =
  result = Moderator()
  if node.kind == JObject:
    if node.hasKey("_id"):
      result.id = to(node["_id"], string)
    if node.hasKey("tenantId"):
      result.tenantId = to(node["tenantId"], string)
    if node.hasKey("name") and node["name"].kind != JNull:
      result.name = some(to(node["name"], typeof(result.name.get())))
    if node.hasKey("userId") and node["userId"].kind != JNull:
      result.userId = some(to(node["userId"], typeof(result.userId.get())))
    if node.hasKey("acceptedInvite"):
      result.acceptedInvite = to(node["acceptedInvite"], bool)
    if node.hasKey("email") and node["email"].kind != JNull:
      result.email = some(to(node["email"], typeof(result.email.get())))
    if node.hasKey("markReviewedCount"):
      result.markReviewedCount = to(node["markReviewedCount"], float64)
    if node.hasKey("deletedCount"):
      result.deletedCount = to(node["deletedCount"], float64)
    if node.hasKey("markedSpamCount"):
      result.markedSpamCount = to(node["markedSpamCount"], float64)
    if node.hasKey("markedNotSpamCount"):
      result.markedNotSpamCount = to(node["markedNotSpamCount"], float64)
    if node.hasKey("approvedCount"):
      result.approvedCount = to(node["approvedCount"], float64)
    if node.hasKey("unApprovedCount"):
      result.unApprovedCount = to(node["unApprovedCount"], float64)
    if node.hasKey("editedCount"):
      result.editedCount = to(node["editedCount"], float64)
    if node.hasKey("bannedCount"):
      result.bannedCount = to(node["bannedCount"], float64)
    if node.hasKey("unFlaggedCount"):
      result.unFlaggedCount = to(node["unFlaggedCount"], float64)
    if node.hasKey("verificationId") and node["verificationId"].kind != JNull:
      result.verificationId = some(to(node["verificationId"], typeof(result.verificationId.get())))
    if node.hasKey("createdAt"):
      result.createdAt = to(node["createdAt"], string)
    if node.hasKey("moderationGroupIds") and node["moderationGroupIds"].kind != JNull:
      result.moderationGroupIds = some(to(node["moderationGroupIds"], typeof(result.moderationGroupIds.get())))

# Custom JSON serialization for Moderator with custom field names
proc `%`*(obj: Moderator): JsonNode =
  result = newJObject()
  result["_id"] = %obj.id
  result["tenantId"] = %obj.tenantId
  if obj.name.isSome():
    result["name"] = %obj.name.get()
  if obj.userId.isSome():
    result["userId"] = %obj.userId.get()
  result["acceptedInvite"] = %obj.acceptedInvite
  if obj.email.isSome():
    result["email"] = %obj.email.get()
  result["markReviewedCount"] = %obj.markReviewedCount
  result["deletedCount"] = %obj.deletedCount
  result["markedSpamCount"] = %obj.markedSpamCount
  result["markedNotSpamCount"] = %obj.markedNotSpamCount
  result["approvedCount"] = %obj.approvedCount
  result["unApprovedCount"] = %obj.unApprovedCount
  result["editedCount"] = %obj.editedCount
  result["bannedCount"] = %obj.bannedCount
  result["unFlaggedCount"] = %obj.unFlaggedCount
  if obj.verificationId.isSome():
    result["verificationId"] = %obj.verificationId.get()
  result["createdAt"] = %obj.createdAt
  if obj.moderationGroupIds.isSome():
    result["moderationGroupIds"] = %obj.moderationGroupIds.get()

