#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type HeaderAccountNotification* = object
  ## 
  id*: string
  title*: string
  message*: string
  messagesByLocale*: Option[Table[string, string]] ## Construct a type with a set of properties K of type T
  dates*: Option[Table[string, string]] ## Construct a type with a set of properties K of type T
  severity*: string
  linkUrl*: Option[string]
  linkText*: Option[string]
  createdAt*: string


# Custom JSON deserialization for HeaderAccountNotification with custom field names
proc to*(node: JsonNode, T: typedesc[HeaderAccountNotification]): HeaderAccountNotification =
  result = HeaderAccountNotification()
  if node.kind == JObject:
    if node.hasKey("_id"):
      result.id = to(node["_id"], string)
    if node.hasKey("title"):
      result.title = to(node["title"], string)
    if node.hasKey("message"):
      result.message = to(node["message"], string)
    if node.hasKey("messagesByLocale") and node["messagesByLocale"].kind != JNull:
      result.messagesByLocale = some(to(node["messagesByLocale"], typeof(result.messagesByLocale.get())))
    if node.hasKey("dates") and node["dates"].kind != JNull:
      result.dates = some(to(node["dates"], typeof(result.dates.get())))
    if node.hasKey("severity"):
      result.severity = to(node["severity"], string)
    if node.hasKey("linkUrl") and node["linkUrl"].kind != JNull:
      result.linkUrl = some(to(node["linkUrl"], typeof(result.linkUrl.get())))
    if node.hasKey("linkText") and node["linkText"].kind != JNull:
      result.linkText = some(to(node["linkText"], typeof(result.linkText.get())))
    if node.hasKey("createdAt"):
      result.createdAt = to(node["createdAt"], string)

# Custom JSON serialization for HeaderAccountNotification with custom field names
proc `%`*(obj: HeaderAccountNotification): JsonNode =
  result = newJObject()
  result["_id"] = %obj.id
  result["title"] = %obj.title
  result["message"] = %obj.message
  if obj.messagesByLocale.isSome():
    result["messagesByLocale"] = %obj.messagesByLocale.get()
  if obj.dates.isSome():
    result["dates"] = %obj.dates.get()
  result["severity"] = %obj.severity
  if obj.linkUrl.isSome():
    result["linkUrl"] = %obj.linkUrl.get()
  if obj.linkText.isSome():
    result["linkText"] = %obj.linkText.get()
  result["createdAt"] = %obj.createdAt

