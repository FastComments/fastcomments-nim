#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_api_status
import model_user_notification

type GetNotificationsResponse* = object
  ## 
  status*: APIStatus
  notifications*: seq[UserNotification]


# Custom JSON deserialization for GetNotificationsResponse with custom field names
proc to*(node: JsonNode, T: typedesc[GetNotificationsResponse]): GetNotificationsResponse =
  result = GetNotificationsResponse()
  if node.kind == JObject:
    if node.hasKey("status"):
      result.status = to(node["status"], APIStatus)
    if node.hasKey("notifications"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["notifications"]
      if arrayNode.kind == JArray:
        result.notifications = @[]
        for item in arrayNode.items:
          result.notifications.add(to(item, UserNotification))

# Custom JSON serialization for GetNotificationsResponse with custom field names
proc `%`*(obj: GetNotificationsResponse): JsonNode =
  result = newJObject()
  result["status"] = %obj.status
  result["notifications"] = %obj.notifications

