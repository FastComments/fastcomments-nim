#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_api_banned_user_with_multi_match_info
import model_api_status

type Code* {.pure.} = enum
  NotFound
  NotLoggedIn

type GetBannedUsersFromCommentResponse* = object
  ## 
  bannedUsers*: seq[APIBannedUserWithMultiMatchInfo]
  code*: Option[Code]
  status*: APIStatus

func `%`*(v: Code): JsonNode =
  result = case v:
    of Code.NotFound: %"not-found"
    of Code.NotLoggedIn: %"not-logged-in"
func `$`*(v: Code): string =
  result = case v:
    of Code.NotFound: $("not-found")
    of Code.NotLoggedIn: $("not-logged-in")

proc to*(node: JsonNode, T: typedesc[Code]): Code =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum Code, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("not-found"):
    return Code.NotFound
  of $("not-logged-in"):
    return Code.NotLoggedIn
  else:
    raise newException(ValueError, "Invalid enum value for Code: " & strVal)


# Custom JSON deserialization for GetBannedUsersFromCommentResponse with custom field names
proc to*(node: JsonNode, T: typedesc[GetBannedUsersFromCommentResponse]): GetBannedUsersFromCommentResponse =
  result = GetBannedUsersFromCommentResponse()
  if node.kind == JObject:
    if node.hasKey("bannedUsers"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["bannedUsers"]
      if arrayNode.kind == JArray:
        result.bannedUsers = @[]
        for item in arrayNode.items:
          result.bannedUsers.add(to(item, APIBannedUserWithMultiMatchInfo))
    if node.hasKey("code") and node["code"].kind != JNull:
      result.code = some(to(node["code"], Code))
    if node.hasKey("status"):
      result.status = to(node["status"], APIStatus)

# Custom JSON serialization for GetBannedUsersFromCommentResponse with custom field names
proc `%`*(obj: GetBannedUsersFromCommentResponse): JsonNode =
  result = newJObject()
  result["bannedUsers"] = %obj.bannedUsers
  if obj.code.isSome():
    result["code"] = %obj.code.get()
  result["status"] = %obj.status

