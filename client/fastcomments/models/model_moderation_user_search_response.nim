#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_api_status
import model_moderation_user_search_projected

type ModerationUserSearchResponse* = object
  ## 
  users*: seq[ModerationUserSearchProjected]
  status*: APIStatus


# Custom JSON deserialization for ModerationUserSearchResponse with custom field names
proc to*(node: JsonNode, T: typedesc[ModerationUserSearchResponse]): ModerationUserSearchResponse =
  result = ModerationUserSearchResponse()
  if node.kind == JObject:
    if node.hasKey("users"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["users"]
      if arrayNode.kind == JArray:
        result.users = @[]
        for item in arrayNode.items:
          result.users.add(to(item, ModerationUserSearchProjected))
    if node.hasKey("status"):
      result.status = to(node["status"], APIStatus)

# Custom JSON serialization for ModerationUserSearchResponse with custom field names
proc `%`*(obj: ModerationUserSearchResponse): JsonNode =
  result = newJObject()
  result["users"] = %obj.users
  result["status"] = %obj.status

