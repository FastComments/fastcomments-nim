#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_api_ticket_file

type APITicketDetail* = object
  ## 
  id*: string
  urlId*: string
  userId*: string
  managedByTenantId*: string
  assignedUserIds*: seq[string]
  subject*: string
  createdAt*: string
  state*: int
  fileCount*: int
  files*: seq[APITicketFile]
  reopenedAt*: Option[string]
  resolvedAt*: Option[string]
  ackAt*: Option[string]


# Custom JSON deserialization for APITicketDetail with custom field names
proc to*(node: JsonNode, T: typedesc[APITicketDetail]): APITicketDetail =
  result = APITicketDetail()
  if node.kind == JObject:
    if node.hasKey("_id"):
      result.id = to(node["_id"], string)
    if node.hasKey("urlId"):
      result.urlId = to(node["urlId"], string)
    if node.hasKey("userId"):
      result.userId = to(node["userId"], string)
    if node.hasKey("managedByTenantId"):
      result.managedByTenantId = to(node["managedByTenantId"], string)
    if node.hasKey("assignedUserIds"):
      result.assignedUserIds = to(node["assignedUserIds"], seq[string])
    if node.hasKey("subject"):
      result.subject = to(node["subject"], string)
    if node.hasKey("createdAt"):
      result.createdAt = to(node["createdAt"], string)
    if node.hasKey("state"):
      result.state = to(node["state"], int)
    if node.hasKey("fileCount"):
      result.fileCount = to(node["fileCount"], int)
    if node.hasKey("files"):
      result.files = to(node["files"], seq[APITicketFile])
    if node.hasKey("reopenedAt") and node["reopenedAt"].kind != JNull:
      result.reopenedAt = some(to(node["reopenedAt"], typeof(result.reopenedAt.get())))
    if node.hasKey("resolvedAt") and node["resolvedAt"].kind != JNull:
      result.resolvedAt = some(to(node["resolvedAt"], typeof(result.resolvedAt.get())))
    if node.hasKey("ackAt") and node["ackAt"].kind != JNull:
      result.ackAt = some(to(node["ackAt"], typeof(result.ackAt.get())))

# Custom JSON serialization for APITicketDetail with custom field names
proc `%`*(obj: APITicketDetail): JsonNode =
  result = newJObject()
  result["_id"] = %obj.id
  result["urlId"] = %obj.urlId
  result["userId"] = %obj.userId
  result["managedByTenantId"] = %obj.managedByTenantId
  result["assignedUserIds"] = %obj.assignedUserIds
  result["subject"] = %obj.subject
  result["createdAt"] = %obj.createdAt
  result["state"] = %obj.state
  result["fileCount"] = %obj.fileCount
  result["files"] = %obj.files
  if obj.reopenedAt.isSome():
    result["reopenedAt"] = %obj.reopenedAt.get()
  if obj.resolvedAt.isSome():
    result["resolvedAt"] = %obj.resolvedAt.get()
  if obj.ackAt.isSome():
    result["ackAt"] = %obj.ackAt.get()

