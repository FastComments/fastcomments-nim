#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_apisso_user

type AddSSOUserAPIResponse* = object
  ## 
  reason*: Option[string]
  code*: Option[string]
  user*: Option[APISSOUser]
  status*: string


# Custom JSON deserialization for AddSSOUserAPIResponse with custom field names
proc to*(node: JsonNode, T: typedesc[AddSSOUserAPIResponse]): AddSSOUserAPIResponse =
  result = AddSSOUserAPIResponse()
  if node.kind == JObject:
    if node.hasKey("reason") and node["reason"].kind != JNull:
      result.reason = some(to(node["reason"], typeof(result.reason.get())))
    if node.hasKey("code") and node["code"].kind != JNull:
      result.code = some(to(node["code"], typeof(result.code.get())))
    if node.hasKey("user") and node["user"].kind != JNull:
      result.user = some(to(node["user"], typeof(result.user.get())))
    if node.hasKey("status"):
      result.status = to(node["status"], string)

# Custom JSON serialization for AddSSOUserAPIResponse with custom field names
proc `%`*(obj: AddSSOUserAPIResponse): JsonNode =
  result = newJObject()
  if obj.reason.isSome():
    result["reason"] = %obj.reason.get()
  if obj.code.isSome():
    result["code"] = %obj.code.get()
  if obj.user.isSome():
    result["user"] = %obj.user.get()
  result["status"] = %obj.status
