#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_api_ban_user_changed_values
import model_api_banned_user

type APIBanUserChangeLog* = object
  ## 
  createdBannedUserId*: Option[string]
  updatedBannedUserId*: Option[string]
  deletedBannedUsers*: Option[seq[APIBannedUser]]
  changedValuesBefore*: Option[APIBanUserChangedValues]


# Custom JSON deserialization for APIBanUserChangeLog with custom field names
proc to*(node: JsonNode, T: typedesc[APIBanUserChangeLog]): APIBanUserChangeLog =
  result = APIBanUserChangeLog()
  if node.kind == JObject:
    if node.hasKey("createdBannedUserId") and node["createdBannedUserId"].kind != JNull:
      result.createdBannedUserId = some(to(node["createdBannedUserId"], typeof(result.createdBannedUserId.get())))
    if node.hasKey("updatedBannedUserId") and node["updatedBannedUserId"].kind != JNull:
      result.updatedBannedUserId = some(to(node["updatedBannedUserId"], typeof(result.updatedBannedUserId.get())))
    if node.hasKey("deletedBannedUsers") and node["deletedBannedUsers"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["deletedBannedUsers"]
      if arrayNode.kind == JArray:
        var arr: seq[APIBannedUser] = @[]
        for item in arrayNode.items:
          arr.add(to(item, APIBannedUser))
        result.deletedBannedUsers = some(arr)
    if node.hasKey("changedValuesBefore") and node["changedValuesBefore"].kind != JNull:
      result.changedValuesBefore = some(to(node["changedValuesBefore"], typeof(result.changedValuesBefore.get())))

# Custom JSON serialization for APIBanUserChangeLog with custom field names
proc `%`*(obj: APIBanUserChangeLog): JsonNode =
  result = newJObject()
  if obj.createdBannedUserId.isSome():
    result["createdBannedUserId"] = %obj.createdBannedUserId.get()
  if obj.updatedBannedUserId.isSome():
    result["updatedBannedUserId"] = %obj.updatedBannedUserId.get()
  if obj.deletedBannedUsers.isSome():
    result["deletedBannedUsers"] = %obj.deletedBannedUsers.get()
  if obj.changedValuesBefore.isSome():
    result["changedValuesBefore"] = %obj.changedValuesBefore.get()

