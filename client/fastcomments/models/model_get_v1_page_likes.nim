#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_api_status

type GetV1PageLikes* = object
  ## 
  urlIdWS*: string
  didLike*: bool
  commentCount*: int
  likeCount*: int
  status*: APIStatus


# Custom JSON deserialization for GetV1PageLikes with custom field names
proc to*(node: JsonNode, T: typedesc[GetV1PageLikes]): GetV1PageLikes =
  result = GetV1PageLikes()
  if node.kind == JObject:
    if node.hasKey("urlIdWS"):
      result.urlIdWS = to(node["urlIdWS"], string)
    if node.hasKey("didLike"):
      result.didLike = to(node["didLike"], bool)
    if node.hasKey("commentCount"):
      result.commentCount = to(node["commentCount"], int)
    if node.hasKey("likeCount"):
      result.likeCount = to(node["likeCount"], int)
    if node.hasKey("status"):
      result.status = to(node["status"], APIStatus)

# Custom JSON serialization for GetV1PageLikes with custom field names
proc `%`*(obj: GetV1PageLikes): JsonNode =
  result = newJObject()
  result["urlIdWS"] = %obj.urlIdWS
  result["didLike"] = %obj.didLike
  result["commentCount"] = %obj.commentCount
  result["likeCount"] = %obj.likeCount
  result["status"] = %obj.status

