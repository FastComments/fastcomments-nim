#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_api_status
import model_moderator

type GetModeratorsResponse* = object
  ## 
  status*: APIStatus
  moderators*: seq[Moderator]


# Custom JSON deserialization for GetModeratorsResponse with custom field names
proc to*(node: JsonNode, T: typedesc[GetModeratorsResponse]): GetModeratorsResponse =
  result = GetModeratorsResponse()
  if node.kind == JObject:
    if node.hasKey("status"):
      result.status = to(node["status"], APIStatus)
    if node.hasKey("moderators"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["moderators"]
      if arrayNode.kind == JArray:
        result.moderators = @[]
        for item in arrayNode.items:
          result.moderators.add(to(item, Moderator))

# Custom JSON serialization for GetModeratorsResponse with custom field names
proc `%`*(obj: GetModeratorsResponse): JsonNode =
  result = newJObject()
  result["status"] = %obj.status
  result["moderators"] = %obj.moderators

