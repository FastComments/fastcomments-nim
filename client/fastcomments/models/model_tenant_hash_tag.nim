#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type TenantHashTag* = object
  ## 
  id*: string
  createdAt*: string
  tenantId*: string
  tag*: string
  url*: Option[string]


# Custom JSON deserialization for TenantHashTag with custom field names
proc to*(node: JsonNode, T: typedesc[TenantHashTag]): TenantHashTag =
  result = TenantHashTag()
  if node.kind == JObject:
    if node.hasKey("_id"):
      result.id = to(node["_id"], string)
    if node.hasKey("createdAt"):
      result.createdAt = to(node["createdAt"], string)
    if node.hasKey("tenantId"):
      result.tenantId = to(node["tenantId"], string)
    if node.hasKey("tag"):
      result.tag = to(node["tag"], string)
    if node.hasKey("url") and node["url"].kind != JNull:
      result.url = some(to(node["url"], typeof(result.url.get())))

# Custom JSON serialization for TenantHashTag with custom field names
proc `%`*(obj: TenantHashTag): JsonNode =
  result = newJObject()
  result["_id"] = %obj.id
  result["createdAt"] = %obj.createdAt
  result["tenantId"] = %obj.tenantId
  result["tag"] = %obj.tag
  if obj.url.isSome():
    result["url"] = %obj.url.get()

