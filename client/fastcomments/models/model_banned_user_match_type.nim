#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type BannedUserMatchType* {.pure.} = enum
  UserId
  Email
  EmailWildcard
  IP

func `%`*(v: BannedUserMatchType): JsonNode =
  result = case v:
    of BannedUserMatchType.UserId: %"userId"
    of BannedUserMatchType.Email: %"email"
    of BannedUserMatchType.EmailWildcard: %"email-wildcard"
    of BannedUserMatchType.IP: %"IP"

func `$`*(v: BannedUserMatchType): string =
  result = case v:
    of BannedUserMatchType.UserId: $("userId")
    of BannedUserMatchType.Email: $("email")
    of BannedUserMatchType.EmailWildcard: $("email-wildcard")
    of BannedUserMatchType.IP: $("IP")

proc to*(node: JsonNode, T: typedesc[BannedUserMatchType]): BannedUserMatchType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum BannedUserMatchType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("userId"):
    return BannedUserMatchType.UserId
  of $("email"):
    return BannedUserMatchType.Email
  of $("email-wildcard"):
    return BannedUserMatchType.EmailWildcard
  of $("IP"):
    return BannedUserMatchType.IP
  else:
    raise newException(ValueError, "Invalid enum value for BannedUserMatchType: " & strVal)

