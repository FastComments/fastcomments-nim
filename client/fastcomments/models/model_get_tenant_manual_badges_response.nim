#
# fastcomments
# 
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
# The version of the OpenAPI document: 0.0.0
# 
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_api_status
import model_tenant_badge

type GetTenantManualBadgesResponse* = object
  ## 
  badges*: seq[TenantBadge]
  status*: APIStatus


# Custom JSON deserialization for GetTenantManualBadgesResponse with custom field names
proc to*(node: JsonNode, T: typedesc[GetTenantManualBadgesResponse]): GetTenantManualBadgesResponse =
  result = GetTenantManualBadgesResponse()
  if node.kind == JObject:
    if node.hasKey("badges"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["badges"]
      if arrayNode.kind == JArray:
        result.badges = @[]
        for item in arrayNode.items:
          result.badges.add(to(item, TenantBadge))
    if node.hasKey("status"):
      result.status = to(node["status"], APIStatus)

# Custom JSON serialization for GetTenantManualBadgesResponse with custom field names
proc `%`*(obj: GetTenantManualBadgesResponse): JsonNode =
  result = newJObject()
  result["badges"] = %obj.badges
  result["status"] = %obj.status

